import './Familia.css'

const Familia = (props) => {
    return (
        <section className='familia'>
            <h3>{props.nome}</h3>
        </section>

    )
}

export default Familia;